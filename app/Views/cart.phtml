<form class="products" action="" method="POST">

    <?php foreach ($cartProducts as $product): ?>

        <table>
            <tr>
                <th><img src="<?= $product->getProduct()->getImg(); ?>" alt=""></th>
                <th><?= $product->getProduct()->getName(); ?></th>
                <td>$ <?= $product->getProduct()->getPrice(); ?></td>
                <td><?= $product->getQuantity(); ?></td>
            </tr>
        </table>

    <?php endforeach; ?>

</form>
<style>

    img {
        height: 70px;
        width: 100px;
    }

    table {
        border-collapse: collapse;
        background: #e0e0e0;
        border: 1px solid #000;
        font-size: 20px;
    }
    td, th {
        padding: 5px;
        border: 1px solid black;
        color: black;
        height: 40px;
        width: 300px;
    }


</style>
